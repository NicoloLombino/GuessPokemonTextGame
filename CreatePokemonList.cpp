#include "CreatePokemonList.h"
#include <iostream>





std::vector<MyPokemon> CreatePkmnList()
{
	std::vector<MyPokemon> MyPokemonList;
	MyPokemonList.push_back({ "Bulbasaur", {grass, poison}, base });
	MyPokemonList.push_back({ "Ivysaur", {grass, poison}, medium });
	MyPokemonList.push_back({ "Venosaur", {grass, poison}, final });
	MyPokemonList.push_back({ "Charmender", {fire}, base });
	MyPokemonList.push_back({ "Charmeleon", {fire}, medium });
	MyPokemonList.push_back({ "Charizard", {fire, flying}, final });
	MyPokemonList.push_back({ "Squirtle", {water}, base });
	MyPokemonList.push_back({ "Wartortle", {water}, medium });
	MyPokemonList.push_back({ "Blastoise", {water}, final });
	MyPokemonList.push_back({ "Caterpie", {bug}, base });
	MyPokemonList.push_back({ "Metapod", {bug}, medium });
	MyPokemonList.push_back({ "Butterfree", {bug, flying}, final });
	MyPokemonList.push_back({ "Weedle", {bug, poison}, base });
	MyPokemonList.push_back({ "Kakuna", {bug, poison}, medium });
	MyPokemonList.push_back({ "Beedrill", {bug, poison}, final });
	MyPokemonList.push_back({ "Pidgey", {normal, flying}, base });
	MyPokemonList.push_back({ "Pidgeotto", {normal, flying}, medium });
	MyPokemonList.push_back({ "Pidgeot", {normal, flying}, final });
	MyPokemonList.push_back({ "Rattata", {normal}, base });
	MyPokemonList.push_back({ "Raticate", {normal}, final });
	MyPokemonList.push_back({ "Spearow", {normal, flying}, base });
	MyPokemonList.push_back({ "Fearow", {normal, flying}, final });
	MyPokemonList.push_back({ "Ekans", {poison}, base });
	MyPokemonList.push_back({ "Arbok", {poison}, final });
	MyPokemonList.push_back({ "Pikachu", {electr}, base });
	MyPokemonList.push_back({ "Raichu", {electr}, final });
	MyPokemonList.push_back({ "Sandshrew", {ground}, base });
	MyPokemonList.push_back({ "Sandslash", {ground}, final });
	MyPokemonList.push_back({ "Nidoran F", {poison}, base });
	MyPokemonList.push_back({ "Nidorina", {poison}, medium });
	MyPokemonList.push_back({ "Nidoqueen", {poison, ground}, final });
	MyPokemonList.push_back({ "Nidoran M", {poison}, base });
	MyPokemonList.push_back({ "Nidorino", {poison}, medium });
	MyPokemonList.push_back({ "Nidoking", {poison, ground}, final });
	MyPokemonList.push_back({ "Clefairy", {normal}, base });
	MyPokemonList.push_back({ "Clefable", {normal}, final });
	MyPokemonList.push_back({ "Vulpix", {fire}, base });
	MyPokemonList.push_back({ "Ninetales", {fire}, final });
	MyPokemonList.push_back({ "Jigglypuff", {normal}, base });
	MyPokemonList.push_back({ "Wigglytuff", {normal}, final });
	MyPokemonList.push_back({ "Zubat", {poison, flying}, base });
	MyPokemonList.push_back({ "Golbat", {poison, flying}, final });
	MyPokemonList.push_back({ "Oddish", {grass, poison}, base });
	MyPokemonList.push_back({ "Gloom", {grass, poison}, medium });
	MyPokemonList.push_back({ "Vileplume", {grass, poison}, final });
	MyPokemonList.push_back({ "Paras", {bug, grass}, base });
	MyPokemonList.push_back({ "Parasect", {bug, grass}, final });
	MyPokemonList.push_back({ "Venonat", {bug, poison}, base });
	MyPokemonList.push_back({ "Venomoth", {bug, poison}, final });
	MyPokemonList.push_back({ "Diglett", {ground}, base });
	MyPokemonList.push_back({ "Dugtrio", {ground}, final });
	MyPokemonList.push_back({ "Meowth", {normal}, base });
	MyPokemonList.push_back({ "Persian", {normal}, final });
	MyPokemonList.push_back({ "Psyduck", {water}, base });
	MyPokemonList.push_back({ "Golduck", {water}, final });
	MyPokemonList.push_back({ "Mankey", {fight}, base });
	MyPokemonList.push_back({ "Primeape", {fight}, final });
	MyPokemonList.push_back({ "Growlithe", {fire}, base });
	MyPokemonList.push_back({ "Arcanine", {fire}, final });
	MyPokemonList.push_back({ "Poliwag", {water}, base });
	MyPokemonList.push_back({ "Poliwhirl", {water}, medium });
	MyPokemonList.push_back({ "Poliwrath", {water}, final });
	MyPokemonList.push_back({ "Abra", {psychc}, base });
	MyPokemonList.push_back({ "Kadabra", {psychc}, medium });
	MyPokemonList.push_back({ "Alakazam", {psychc}, final });
	MyPokemonList.push_back({ "Machop", {fight}, base });
	MyPokemonList.push_back({ "Machoke", {fight}, medium });
	MyPokemonList.push_back({ "Machamp", {fight}, final });
	MyPokemonList.push_back({ "Bellsprout", {grass, poison}, base });
	MyPokemonList.push_back({ "Weepinbell", {grass, poison}, medium });
	MyPokemonList.push_back({ "Victreebel", {grass, poison}, final });
	MyPokemonList.push_back({ "Tentacool", {water, poison}, base });
	MyPokemonList.push_back({ "Tentacruel", {water, poison}, final });
	MyPokemonList.push_back({ "Geodude", {rock, ground}, base });
	MyPokemonList.push_back({ "Graveler", {rock, ground}, medium });
	MyPokemonList.push_back({ "Golem", {rock, ground}, final });
	MyPokemonList.push_back({ "Ponyta", {fire}, base });
	MyPokemonList.push_back({ "Rapidash", {fire}, final });
	MyPokemonList.push_back({ "Slowpoke", {water, psychc}, base });
	MyPokemonList.push_back({ "Slowbro", {water, psychc}, final });
	MyPokemonList.push_back({ "Magnemite", {electr, steel}, base });
	MyPokemonList.push_back({ "Magneton", {electr, steel}, final });
	MyPokemonList.push_back({ "Farfetch'd", {normal, flying}, final });
	MyPokemonList.push_back({ "Doduo", {normal, flying}, base });
	MyPokemonList.push_back({ "Dodrio", {normal, flying}, final });
	MyPokemonList.push_back({ "Seal", {water}, base });
	MyPokemonList.push_back({ "Dewgong", {water, ice}, final });
	MyPokemonList.push_back({ "Grimer", {poison}, base });
	MyPokemonList.push_back({ "Muk", {poison}, final });
	MyPokemonList.push_back({ "Shellder", {water}, base });
	MyPokemonList.push_back({ "Cloyster", {water, ice}, final });
	MyPokemonList.push_back({ "Gastly", {ghost, poison}, base });
	MyPokemonList.push_back({ "Haunter", {ghost, poison}, medium });
	MyPokemonList.push_back({ "Gengar", {ghost, poison}, final });
	MyPokemonList.push_back({ "Onix", {rock, ground}, final });
	MyPokemonList.push_back({ "Drowzee", {psychc}, base });
	MyPokemonList.push_back({ "Hypno", {psychc}, final });
	MyPokemonList.push_back({ "Krabby", {water}, base });
	MyPokemonList.push_back({ "Kingler", {water}, final });
	MyPokemonList.push_back({ "Voltorb", {electr}, base });
	MyPokemonList.push_back({ "Electrode", {electr}, final });
	MyPokemonList.push_back({ "Exeggcute", {grass, psychc}, base });
	MyPokemonList.push_back({ "Exeggcutor", {grass, psychc}, final });
	MyPokemonList.push_back({ "Cubone", {ground}, base });
	MyPokemonList.push_back({ "Marowak", {ground}, final });
	MyPokemonList.push_back({ "Hitmonlee", {fight}, final });
	MyPokemonList.push_back({ "Hitmonchan", {fight}, final });
	MyPokemonList.push_back({ "Lickitung", {normal}, final });
	MyPokemonList.push_back({ "Koffing", {poison}, base });
	MyPokemonList.push_back({ "Weezing", {poison}, final });
	MyPokemonList.push_back({ "Rhyhorn", {ground, rock}, base });
	MyPokemonList.push_back({ "Rhydon", {ground, rock}, final });
	MyPokemonList.push_back({ "Chansey", {normal}, final });
	MyPokemonList.push_back({ "Tangela", {grass}, final });
	MyPokemonList.push_back({ "Kangaskhan", {normal}, final });
	MyPokemonList.push_back({ "Horsea", {water}, base });
	MyPokemonList.push_back({ "Seadra", {water}, final });
	MyPokemonList.push_back({ "Goldeen", {water}, base });
	MyPokemonList.push_back({ "Seaking", {water}, final });
	MyPokemonList.push_back({ "Staryu", {water}, base });
	MyPokemonList.push_back({ "Starmie", {water, psychc}, final });
	MyPokemonList.push_back({ "Mr. Mime", {psychc}, final });
	MyPokemonList.push_back({ "Scyther", {bug, flying}, final });
	MyPokemonList.push_back({ "Jinx", {ice, psychc}, final });
	MyPokemonList.push_back({ "Electabuzz", {electr}, final });
	MyPokemonList.push_back({ "Magmar", {fire}, final });
	MyPokemonList.push_back({ "Pinsir", {bug}, final });
	MyPokemonList.push_back({ "Tauros", {normal}, final });
	MyPokemonList.push_back({ "Magikarp", {water}, base });
	MyPokemonList.push_back({ "Gyarados", {water, flying}, final });
	MyPokemonList.push_back({ "Lapras", {water, ice}, final });
	MyPokemonList.push_back({ "Ditto", {normal}, final });
	MyPokemonList.push_back({ "Eevee", {normal}, base });
	MyPokemonList.push_back({ "Vaporeon", {water}, final });
	MyPokemonList.push_back({ "Jolteon", {electr}, final });
	MyPokemonList.push_back({ "Flareon", {fire}, final });
	MyPokemonList.push_back({ "Porygon", {normal}, final });
	MyPokemonList.push_back({ "Omanyte", {rock, water}, base });
	MyPokemonList.push_back({ "Omastar", {rock, water}, final });
	MyPokemonList.push_back({ "Kabuto", {rock, water}, base });
	MyPokemonList.push_back({ "Kabutops", {rock, water}, final });
	MyPokemonList.push_back({ "Aerodactyl", {rock, flying}, final });
	MyPokemonList.push_back({ "Snorlax", {normal}, final });
	MyPokemonList.push_back({ "Articuno", {ice, flying}, final });
	MyPokemonList.push_back({ "Zapdos", {electr, flying}, final });
	MyPokemonList.push_back({ "Moltres", {fire, flying}, final });
	MyPokemonList.push_back({ "Dratini", {dragon}, base });
	MyPokemonList.push_back({ "Dragonair", {dragon}, medium });
	MyPokemonList.push_back({ "Dragonite", {dragon, flying}, final });
	MyPokemonList.push_back({ "Mewtwo", {psychc}, final });
	MyPokemonList.push_back({ "Mew", {psychc}, final });

	return MyPokemonList;
}
